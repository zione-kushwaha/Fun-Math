import 'package:flutter/material.dart';

class FunMathApp extends StatelessWidget {
  const FunMathApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      debugShowCheckedModeBanner: false,
      title: 'Fun Math',
     home: Scaffold(),
    );
  }
}